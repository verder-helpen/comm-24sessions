// External URL for the 24Sessions ID Contact plugin, in order for the 
// application to fetch the attributes.
window.SERVER_URL = 'https://comm-24sessions.idcontact.test.tweede.golf'
window.TRANSLATIONS = {
  nl: {
    // attribute keys
    email: 'email',
    fullname: 'naam',
    city: 'woonplaats',
  }
};